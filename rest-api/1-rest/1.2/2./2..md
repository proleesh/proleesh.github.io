---
description: '작성자: 이성혁'
---

# 2. 표현을 통한 리소스 처리

클라이언트는 리소스 표현을 처리한다. 같은 리소스라도 다른 클라이언트에서는 다른 방식으로 표현될 수 있다. 예를 들어, 하나의 document가 웹 브라우저에서 HTML로 특정 프로그램에서는 JSON으로 표현 될 수도 있다. 여기서 중요한 점은 표현은 리소스와 상호작용하는 방법이지 리소스 그 자체는 아니라는 것이다. 이러한 개념적 구별에 의해 리소스는 다른 방식과 형식으로 표현되지만 식별자 자체는 변하지 않는다.
