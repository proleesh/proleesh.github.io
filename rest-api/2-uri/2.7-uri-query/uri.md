---
description: '작성자: 이성혁'
---

# 규칙: URI 쿼리 부분으로 컬렉션이나 스토어를 필터링할 수 있다

URI 쿼리는 컬렉션이나 스토어의 검색 기준으로 사용하기에 적합하다. 다음 예를 보다.

> GET /users 1️⃣
>
> GET /users?role=admin

1️⃣ 응답 메시지의 상태 표현은 컬렉션에 있는 모든 사용자의 리스트다.

2️⃣ 응답 메시지의 상태 표현은 컬렉션에 있는 사용자 중 'role'의 값이 'admin'인 사용자의 리스트다.
